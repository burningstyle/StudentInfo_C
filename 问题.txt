为什么char str[]数组scanf就能读取，并存入。但是char *str就不能用scanf输入……

前面一直是遍历到最后一个元素的时候总是出错（因为采用的是前插入），判断应该是头结点设置错误,建立头结点的函数原型为int newDataBase(studentLink *head),想法是传入一个指针，用函数体内的动态分配为这个指针分配地址空间，然后用整数返回结果是顺利执行还是执行失败。但是怎样改函数体与遍历函数，都没有用。最后将函数原型改为 studentLink *newDataBase(),这样就可以了，怀疑是以前原型指针参数的问题……，是不是应该是个二维的指针才行。。。。。现。在试试。二维指针果然可以，定义原型为int newDataBase(studentLink **head)。
